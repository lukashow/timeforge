import { PlaceholderPage } from './PlaceholderPage'

export function UsersPage() {
  return (
    <PlaceholderPage
      titleKey="pages.users"
      defaultTitle="用户管理"
      icon="tabler:user-cog"
      descriptionKey="pages.users_desc"
      defaultDescription="管理系统用户和权限"
    />
  )
}
